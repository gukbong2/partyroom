<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.mapper.ReservMapper">

<select id="getList" resultType="com.spring.domain.ReservVO" >
	<![CDATA[	
		SELECT * FROM RESERVATION WHERE RESERV_IDX > 0 ORDER BY RESERV_IDX DESC
	]]>
</select>

<insert id="insert">

	<!-- <selectKey keyProperty="reserv_idx" order="BEFORE" resultType="long">
		SELECT SEQ_RESERVATION.NEXTVAL FROM DUAL
	</selectKey> -->
	
	INSERT INTO RESERVATION(RESERV_IDX, PRICE, BREAKFAST, RESERV_NAME, START_DATE, END_DATE, MALE, FEMALE, Ticket, BILLING_DATE)
		VALUES (SEQ_RESERVATION.NEXTVAL, #{price}, #{breakfast}, #{reserv_name}, #{start_date}, #{end_date}, #{male}, #{female}, #{ticket}, SYSDATE)

</insert>

<select id="read" resultType="com.spring.domain.ReservVO">
	SELECT * FROM RESERVATION WHERE RESERV_IDX = #{reserv_idx}
</select>

<delete id="delete">
	DELETE FROM RESERVATION WHERE RESERV_IDX = #{reserv_idx}
</delete>

<!-- 
수정은 할게많아서 나중에 삭제까지만
<update id="update">

</update> -->














</mapper>
